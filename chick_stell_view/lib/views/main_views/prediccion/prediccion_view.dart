

import 'package:flutter/material.dart';

class PrediccionView extends StatelessWidget {
  const PrediccionView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Prediccion View'),
      ),
    );
  }
}