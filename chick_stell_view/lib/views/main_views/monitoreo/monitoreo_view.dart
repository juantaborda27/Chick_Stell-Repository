

import 'package:flutter/material.dart';

class MonitoreoView extends StatelessWidget {
  const MonitoreoView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Monitoreo  View'),
      ),
    );
  }
}