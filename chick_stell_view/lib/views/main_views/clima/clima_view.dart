

import 'package:flutter/material.dart';

class ClimaView extends StatelessWidget {
  const ClimaView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Clima View'),
      ),
    );
  }
}